spring.application.name=GeekSphere-BE
spring.data.rest.base-path=api


server.reactive.session.cookie.same-site=strict

# application.properties
server.port=${APPLICATION_PORT}
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.data.mongodb.uri=${MONGODB_URI}

spring.data.redis.host=${REDIS_HOST}
spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.port=${REDIS_PORT}

# Application Settings
APPLICATION_PORT=8080

# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5431/auth
DATABASE_USERNAME=test
DATABASE_PASSWORD=1234

# mongodb Configuration
MONGODB_URI=mongodb+srv://testuser:geek1234@geeksphere.i8re5n9.mongodb.net/geeksphere?retryWrites=true&w=majority&appName=GeekSphere

REDIS_HOST=localhost
REDIS_PASSWORD=1234
REDIS_PORT=6379

# Eureka Configuration
EUREKA_SERVER_URL=http://localhost:8761/eureka/

EMAIL_SERVICE_NAME=localhost

# Cloudinary Configuration
cloudinary.cloud_name=djcljzfxz
cloudinary.api_key=835974465955333
cloudinary.api_secret=Od0D3_2h1g43Gi4TUHNfj6tUFr0

# File Upload Configuration
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect